import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { ProgressBar } from 'ui';

<Meta title="Base/ProgressBar" component={ProgressBar} />

export const Template = (args) => {
  return <ProgressBar fill={60} {...args} />;
};

export const TemplateSeaGreen = (args) => {
  return <ProgressBar fill={40} appearance="seaGreen" {...args} />;
};

# ProgressBar

ProgressBar description

```tsx
<ProgressBar label="ProgressBar" />
```

<Canvas>
  <Story name="ProgressBar">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="ProgressBar" />

### ProgressBar SeaGreen

<Canvas>
  <Story name="ProgressBarSeaGreen">{TemplateSeaGreen.bind({})}</Story>
</Canvas>

