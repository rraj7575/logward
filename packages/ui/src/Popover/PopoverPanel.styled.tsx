import { PadBox } from '@bedrock-layout/padbox';
import styled from 'styled-components';

export const PopoverPanelPadboxStyled = styled(PadBox)`
  background: var(--color-white);
  border-radius: 8px;
  box-shadow: 0px 0px 16px rgba(0, 0, 0, 0.15);
`;
