import { PadBox } from '@bedrock-layout/padbox';
import styled from 'styled-components';

export const PopoverMenuPadboxStyled = styled(PadBox)`
  background: var(--color-white);
  border-radius: 12px;
  box-shadow: 0px 0px 16px rgba(0, 0, 0, 0.15);
`;
