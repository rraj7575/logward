import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';
import { HiTemplate } from 'react-icons/hi';

import { RoundButton } from './RoundButton';

<Meta title="Base/RoundButton" component={RoundButton} />

export const Template = (args) => {
  const [active, setActive] = useState(false);
  return (
    <RoundButton
      onClick={() => {
        setActive(!active);
      }}
      active={active}
    >
      1
    </RoundButton>
  );
};

export const TemplateDisabled = (args) => (
  <RoundButton disabled>1</RoundButton>
);

# Icon Button

Icon Button description here

```tsx
<RoundButton />
```

<Canvas>
  <Story name="Round Button">{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story name="Round Button Disabled">{TemplateDisabled.bind({})}</Story>
</Canvas>
