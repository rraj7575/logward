import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { HiTemplate } from 'react-icons/hi';

import { IconButton } from './IconButton';

<Meta title="Base/IconButton" component={IconButton} />

export const Template = (args) => (
  <IconButton>
    <HiTemplate />
  </IconButton>
);

# Icon Button

Icon Button description here

```tsx
<IconButton />
```

<Canvas>
  <Story name="Icon Button">{Template.bind({})}</Story>
</Canvas>
