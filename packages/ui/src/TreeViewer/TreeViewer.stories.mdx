import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Button } from 'ui';
import { TreeViewer } from 'ui';

import { PopoverPanel } from './../Popover';
import { dataset } from './fixtures/dataset';

<Meta title="Base/TreeViewer" component={TreeViewer} />

export const Template = (args) => {
  return (
    <div
      style={{
        height: 600,
        display: 'flex',
        justifyContent: 'start',
        alignItems: 'center',
        flexDirection: 'column',
      }}
    >
      <PopoverPanel trigger="click">
        <TreeViewer
          {...args}
          dataset={dataset}
          onClick={(obj) => {
            console.log(obj);
          }}
        />
      </PopoverPanel>
    </div>
  );
};

export const TemplateWithSearch = (args) => {
  return (
    <div
      style={{
        height: 600,
        display: 'flex',
        justifyContent: 'start',
        alignItems: 'center',
        flexDirection: 'column',
      }}
    >
      <PopoverPanel trigger="click">
        <TreeViewer
          {...args}
          showSearchField={true}
          dataset={dataset}
          onClick={(obj) => {
            console.log(obj);
          }}
        />
      </PopoverPanel>
    </div>
  );
};

# TreeViewer

TreeViewer here

```tsx
<TreeViewer label="TreeViewer" />
```

<Canvas>
  <Story name="TreeViewer">{Template.bind({})}</Story>
</Canvas>

```tsx
<TreeViewer label="TreeViewer" />
```

<Canvas>
  <Story name="TreeViewerWithSearch">{TemplateWithSearch.bind({})}</Story>
</Canvas>

<ArgsTable story="TreeViewer" />
