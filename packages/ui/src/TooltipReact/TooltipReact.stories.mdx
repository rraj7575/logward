import { Inline } from '@bedrock-layout/inline';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { ThemeProvider } from 'styled-components';

import { Button } from '../Button';
import { TooltipReact } from './TooltipReact';

<Meta
  title="Base/TooltipReact"
  component={TooltipReact}
  argTypes={{
    size: {
      name: 'size',
      options: ['small', 'medium', 'large'],
      control: {
        type: 'select',
      },
      defaultValue: 'small',
    },
  }}
/>

export const Template = () => {
  return (
    <Inline gutter="5rem" justify="center" style={{ height: '200px' }}>
      <TooltipReact id="tooltip1">
        <Button>Small Size TooltipReact</Button>
      </TooltipReact>
      <TooltipReact id="tooltip2">
        <Button>Medium Size TooltipReact</Button>
      </TooltipReact>
      <TooltipReact id="tooltip3">
        <Button>Large Size TooltipReact</Button>
      </TooltipReact>
    </Inline>
  );
};

# TooltipReact

TooltipReact description here

```tsx
<TooltipReact label="TooltipReact" />
```

### Tooltip of small size (Default)

<Canvas>
  <Story name="TooltipReact">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="TooltipReact" />
