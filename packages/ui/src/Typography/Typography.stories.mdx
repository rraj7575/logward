import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { ThemeProvider } from 'styled-components';
import { Typography, typographyElements } from './Typography';

<Meta
  title="Base/Typography"
  component={Typography}
  argTypes={{
    name: {
      name: 'name',
      options: Object.keys(typographyElements).sort(),
      control: {
        type: 'select',
      },
    },
  }}
/>

export const Template = (args) => (
  <Typography {...args}>
    The quick brown fox jumps over the lazy dog!
  </Typography>
);

# Typography

Typography description here

```tsx
<Typography>I'm some paragraph</Typography>
```

<Canvas>
  <Story
    name="Typography"
    args={{
      children: "I'm some paragraph",
      name: 'paragraph',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Typography" />
