import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';
import { useState, useEffect } from 'react';

import { CheckboxField } from 'ui';

<Meta
  title="Forms/Fields/CheckboxField"
  component={CheckboxField}
  argTypes={{
    label: {
      control: {
        type: 'text',
      },
    },
  }}
/>

export const Template = (args) => {
  const { control } = useForm();
  return <CheckboxField control={control} {...args} name="isData" />;
};

# CheckboxField

CheckboxField description here

```tsx
<CheckboxField label="CheckboxField" />
```

<Canvas>
  <Story name="CheckboxField">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="CheckboxField" />
