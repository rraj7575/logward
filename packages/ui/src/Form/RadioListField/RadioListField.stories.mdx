import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';

import { RadioListField } from 'ui';

<Meta title="Forms/Fields/RadioListField" component={RadioListField} />

export const Template = (args) => {
  const { control } = useForm();
  return (
    <RadioListField
      name="gender"
      defaultValue="male"
      options={[
        {
          label: 'Male',
          value: 'male',
          icon: 'https://img.icons8.com/bubbles/256/user-male.png',
        },
        {
          label: 'Female',
          value: 'female',
          icon: 'https://img.icons8.com/bubbles/256/user-female.png',
        },
      ]}
      control={control}
      {...args}
    />
  );
};

# RadioListField

RadioListField description

```tsx
<RadioListField label="RadioListField" />
```

<Canvas>
  <Story name="RadioListField">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="RadioListField" />
