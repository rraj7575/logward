import { useState, useEffect } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { CheckboxInput } from './CheckboxInput';

<Meta title="Forms/Input/CheckboxInput" component={CheckboxInput} />

export const CheckboxStateful = (args) => {
  const [value, setValue] = useState(args?.checked);
  useEffect(() => {
    setValue(args.checked);
  }, [args?.checked]);
  return (
    <CheckboxInput
      {...args}
      checked={value}
      onChange={() => setValue(!value)}
    />
  );
};

# CheckboxInput

CheckboxInput description here

```tsx
<CheckboxInput name="CheckboxInput" />
```

export const Template = (args) => <CheckboxStateful {...args} />;

<Canvas>
  <Story
    name="CheckboxInput"
    args={{
      label: '',
      useId: 'checkbox',
      checked: false,
      appearance: 'checkbox'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
