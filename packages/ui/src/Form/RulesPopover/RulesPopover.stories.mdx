import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { RulesPopover } from 'ui';

import { rules } from '../fixtures/rules';

<Meta title="Forms/Input/RulesPopover" component={RulesPopover} />

export const Template = (args) => {
  return <RulesPopover options={rules} {...args} />;
};

# RulesPopover

RulesPopover description

```tsx
<RulesPopover label="RulesPopover" />
```

<Canvas>
  <Story name="RulesPopover">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="RulesPopover" />
