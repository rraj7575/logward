import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';
import { Inline, PadBox } from '@bedrock-layout/primitives';
import { ExpandingTextField } from 'ui';

<Meta title="Forms/Fields/ExpandingTextField" component={ExpandingTextField} />

export const Template = (args) => {
  const { control } = useForm();
  return (
    <Inline>
      <PadBox
        padding={[5]}
        style={{
          border: '1px solid var(--color-dodgerBlue)',
          borderRadius: '4px',
        }}
      >
        <ExpandingTextField
          defaultValue="I'm an expanding input, try it!"
          control={control}
          name="ruleName"
          {...args}
        />
      </PadBox>
    </Inline>
  );
};

# ExpandingTextField

ExpandingTextField description here

```tsx
<ExpandingTextField label="ExpandingTextField" />
```

<Canvas>
  <Story name="ExpandingTextField">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="ExpandingTextField" />
