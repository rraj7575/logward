import { useState, useEffect } from 'react';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { DropdownInput } from './DropdownInput';

<Meta title="Forms/Input/DropdownInput" component={DropdownInput} />

# DropdownInput

DropdownInput description here

```tsx
<DropdownInput name="DropdownInput" />
```

export const Template = (args) => (
  <div style={{ width: '408px', height: '150px' }}>
    <DropdownInput {...args} />
  </div>
);

<Canvas>
  <Story
    name="DropdownInput"
    args={{
      name: 'select',
      placeholder: 'Select color',
      options: [
        {
          label: 'Red',
          value: 'red',
        },
        {
          label: 'Black',
          value: 'black',
        },
      ],
      onChange: () => {},
      isMulti: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
