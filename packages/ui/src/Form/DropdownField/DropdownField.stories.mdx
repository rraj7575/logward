import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';
import { useState, useEffect } from 'react';

import { DropdownField } from 'ui';

<Meta title="Forms/Fields/DropdownField" component={DropdownField} />

export const Template = (args) => {
  const { control } = useForm();
  return (
    <div style={{ width: '408px', height: '150px' }}>
      <DropdownField control={control} {...args} />
    </div>
  );
};

# DropdownField

DropdownField description here

```tsx
<DropdownField label="DropdownField" />
```

<Canvas>
  <Story
    name="DropdownField"
    args={{
      name: 'select',
      placeholder: 'Select color',
      options: [
        {
          label: 'Red',
          value: 'red',
        },
        {
          label: 'Black',
          value: 'black',
        },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="DropdownField" />
