import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';
import { Inline } from '@bedrock-layout/inline';

import { rules } from '../fixtures/rules';
import { RulesPopoverField } from 'ui';

<Meta title="Forms/Fields/RulesPopoverField" component={RulesPopoverField} />

export const Template = (args) => {
  const { control } = useForm();
  return (
    <Inline>
      <RulesPopoverField control={control} {...args} />
    </Inline>
  );
};

# RulesPopoverField

RulesPopoverField description here

```tsx
<RulesPopoverField label="RulesPopoverField" />
```

<Canvas>
  <Story
    name="RulesPopoverField"
    args={{
      name: 'Rulespopover',
      options: rules,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="RulesPopoverField" />
