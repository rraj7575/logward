import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { DateInput } from 'ui';
import { useState } from 'react';

<Meta title="Forms/Input/DateInput" component={DateInput} />

export const Template = (args) => {
  const [value, setValue] = useState();
  return (
    <div style={{ width: '400px', height: '500px' }}>
      <DateInput value={value} onChange={setValue} {...args} />
    </div>
  );
};

# DateInput

DateInput description here

```tsx
<DateInput label="DateInput" />
```

<Canvas>
  <Story
    name="DateInput"
    args={{
      fromDate: new Date(),
      toDate: new Date("2023-06-08")
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="DateInput" />
