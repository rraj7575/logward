import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { useForm } from 'react-hook-form';
import { useState, useEffect } from 'react';

import { CheckboxGroupField } from 'ui';

<Meta
  title="Forms/Fields/CheckboxGroupField"
  component={CheckboxGroupField}
  argTypes={{
    label: {
      control: {
        type: 'text',
      },
    },
  }}
/>

export const Template = (args) => {
  const { control } = useForm();
  return (
    <CheckboxGroupField
      name="gender"
      defaultValues={['male']}
      options={[
        {
          label: 'Male',
          value: 'male',
          icon: 'https://img.icons8.com/bubbles/256/user-male.png',
        },
        {
          label: 'Female',
          value: 'female',
          icon: 'https://img.icons8.com/bubbles/256/user-female.png',
        },
        {
          label: 'Others',
          value: 'others',
        },
      ]}
      control={control}
      {...args}
    />
  );
};

# CheckboxGroupField

CheckboxGroupField description

```tsx
<CheckboxGroupField label="CheckboxGroupField" />
```

<Canvas>
  <Story name="CheckboxGroupField">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="CheckboxGroupField" />
