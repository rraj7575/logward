import { PadBox } from '@bedrock-layout/primitives';
import styled from 'styled-components';

export const OutputPanelContainer = styled(PadBox)`
  background: var(--color-lightGray);
  border-radius: 4px;
`;
