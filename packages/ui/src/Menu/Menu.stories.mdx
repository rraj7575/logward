import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useState } from 'react';
import { Menu } from './Menu';
import { MenuItem } from './MenuItem';
import { Typography } from '../Typography';

<Meta title="Base/Menu" component={Menu} />

export const Template = (args) => {
  const [selectedItem, setSelectedItem] = useState('one');
  const menuList = {
    one: 'Menu Item One',
    two: 'Menu Item Two',
  };
  return (
    <Menu
      onMenuItemClick={(value) => {
        setSelectedItem(value);
      }}
      label={menuList[selectedItem]}
    >
      <MenuItem value="one">
        <Typography>Menu Item One</Typography>
      </MenuItem>
      <MenuItem value="two">
        <Typography>Menu Item Two</Typography>
      </MenuItem>
    </Menu>
  );
};

# Menu

This is the menu componenet which is being used throughout the application mostly in tables

```tsx
<Menu />
```

<Canvas>
  <Story name="Menu">{Template.bind({})}</Story>
</Canvas>
